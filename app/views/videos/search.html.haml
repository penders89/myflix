%section.genre
  %header.clearfix
    %h1 Search results
  %article.row
    - if @videos.empty?
      %p Sorry no results matched your query
    - else
      - @videos.each do |video|
        .video.col-md-2
          = link_to video_path(video) do 
            %img(src="#{video.small_cover}")
